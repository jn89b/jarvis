# Jarvis

## Installation
```
git submodule update --init --recursive
pip install -r requirements.txt
```

# Run sim
```
python3 train_rl_high_speed_env.py
```

# Notes
- The config directory should have yaml files that are set for configurations of the environment and the agents